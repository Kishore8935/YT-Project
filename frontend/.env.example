# Backend API base URL exposed to the browser bundle.
# Must be prefixed BUN_PUBLIC_ so Bun inlines it at build time.
# Local dev can omit this (config.ts falls back to http://localhost:3000).
BUN_PUBLIC_API_URL=http://localhost:3000
